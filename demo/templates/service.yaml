apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.demo.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.demo.name }}
  ports:
    - name: apiport
      protocol: TCP
      port: 80
      targetPort: {{ .Values.demo.apiport }}
